name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Match the project's minimum supported interpreter (requires-python >= 3.12).
  UV_PYTHON: "3.12"

jobs:
  checks:
    runs-on: ubuntu-latest
    # Backstop against a hung job (the whole run is normally well under 2 min);
    # pytest-timeout catches per-test hangs, this bounds anything it can't.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync

      # The same four checks prek runs on commit: ruff format, ruff check,
      # basedpyright (strict), and pytest.
      - name: Run all checks
        run: uv run prek run --all-files --show-diff-on-failure
