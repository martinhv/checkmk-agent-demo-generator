__pycache__/
*.pyc

# dev toolchain (uv venv + tool caches)
.venv/
.coverage
htmlcov/
.pytest_cache/
.ruff_cache/

# Build/packaging artifacts — never commit archives
*.tar
*.tar.gz
*.tgz
*.tar.xz
*.tar.bz2
*.zip
__pycache__/

# SNMP stored-walk output files (netsim --transport walk / --once, if pointed
# at the repo root). The default live-SNMP transport writes none; these are
# generated artifacts named <device>.<ESTATE_DOMAIN>, never source.
/*.corp.meridian-retail.com
